import { IUser } from '../../store';

export const createNewUser = (variables: Partial<IUser>) => {
    // send to the server

    // return created user
    return {};
};

export const updateUser = (variables: Partial<IUser>) => {
    // send request to server

    // return updated user
    return {};
};
