import { IUser } from '../../store';

export const getUser = (variables: Partial<IUser>) => {
    // send to the server

    // return user
    return {};
};

export const getUsers = () => {
    // send request to server

    // return users
    return {};
};
